import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Flex from './Flex';

<Meta
  title="Components/Flex"
  component={Flex}
  argTypes={{
    direction: { control: 'select', options: ['row', 'column'], description: 'Sets the direction of items in the flex container.' },
    justify: { control: 'select', options: ['flex-start', 'center', 'flex-end'], description: 'Aligns the items along the main axis.' },
    align: { control: 'select', options: ['flex-start', 'center', 'flex-end'], description: 'Aligns the items along the cross axis.' },
    gap: { control: 'number', description: 'Gap between each item in the flex container.' }
  }}
  args={{
    direction: 'row',
    justify: 'center',
    align: 'center',
    gap: 10
  }}
/>

# Flex Component
Flex is a highly configurable layout component designed to facilitate the arrangement of child elements along a single or multiple axes. It employs CSS Flexbox under the hood, enabling you to effortlessly control the alignment, direction, and spacing of your content.

## Properties

<ArgsTable of={Flex} />

## Live Examples

<Canvas>
  <Story name="Component">
    {args => (
      <Flex {...args}>
        <div>Item 1</div>
        <div>Item 2</div>
        <div>Item 3</div>
      </Flex>
    )}
  </Story>
</Canvas>
