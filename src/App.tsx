import React from 'react';
import { Container, Flex, Grid } from './lib';

function App() {
  return (
    <Container maxWidth={800} minWidth={800}>
      <div>Content</div>
    </Container>
  );
}

export default App;
